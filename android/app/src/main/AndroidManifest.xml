<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 自己更新 (Updater)。初回だけ「不明なアプリのインストール」をユーザーが許可する -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- 接続コードの QR 読み取り (QrScannerActivity)。カメラが無い端末でも入れられるように required=false -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".ChipRemoteApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChipRemote">
        <!-- unbundled ML Kit: インストール時にバーコードモデルを Play 開発者サービスに取得させる -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="barcode" />


        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".QrScannerActivity"
            android:exported="false"
            android:theme="@style/Theme.ChipRemote.NoActionBar" />

        <service
            android:name=".ChipMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <receiver
            android:name=".ActionReceiver"
            android:exported="false" />

        <!-- PackageInstaller セッションの結果 (明示 PendingIntent で届くので export 不要) -->
        <receiver
            android:name=".InstallResultReceiver"
            android:exported="false" />

        <!-- google-services.json を使わず ChipRemoteApp で手動初期化するので、自動初期化の provider を外す。
             残すと起動時に default FirebaseApp の生成を試みて (設定が無いので) ログに失敗を出す。 -->
        <provider
            android:name="com.google.firebase.provider.FirebaseInitProvider"
            android:authorities="${applicationId}.firebaseinitprovider"
            tools:node="remove" />
    </application>
</manifest>
